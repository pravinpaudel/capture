<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme matching app icon colors -->
    <style name="Theme.Capture" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Primary brand color - Blue from icon -->
        <item name="colorPrimary">@color/app_primary</item>
        <item name="colorPrimaryVariant">@color/app_primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color - Complementary blue -->
        <item name="colorSecondary">@color/app_accent</item>
        <item name="colorSecondaryVariant">@color/app_primary_dark</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Surface and background colors -->
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">#212121</item>
        <item name="android:colorBackground">@color/app_background</item>
        <!-- Status bar color - transparent for fullscreen -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <!-- Material button styles -->
        <item name="materialButtonStyle">@style/Widget.Capture.Button</item>
    </style>
    
    <!-- Theme for EventFormActivity without fullscreen to support keyboard adjustResize -->
    <style name="Theme.Capture.NoFullscreen" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- No fullscreen mode -->
        <item name="android:windowFullscreen">false</item>
        <!-- Primary brand color - Blue from icon -->
        <item name="colorPrimary">@color/app_primary</item>
        <item name="colorPrimaryVariant">@color/app_primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color - Complementary blue -->
        <item name="colorSecondary">@color/app_accent</item>
        <item name="colorSecondaryVariant">@color/app_primary_dark</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Surface and background colors -->
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">#212121</item>
        <item name="android:colorBackground">@color/app_background</item>
        <!-- Status bar -->
        <item name="android:statusBarColor">@color/app_primary</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
        <!-- Material button styles -->
        <item name="materialButtonStyle">@style/Widget.Capture.Button</item>
    </style>
    
    <!-- Button style with rounded corners -->
    <style name="Widget.Capture.Button" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">8dp</item>
        <item name="android:textAllCaps">false</item>
    </style>
</resources>